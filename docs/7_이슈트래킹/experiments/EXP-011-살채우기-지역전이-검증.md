# EXP-011 — '살' 채우기의 지역 전이 검증: 권역 숨기기 총연습 (사전등록 — 개정 5 재동결)

> 읽기 안내: "결정 체인 D2 경기·부산·전북"·"0단계 시도·센서스 2%"·"1단계 300명·풀 프로필"·"개정 4의 200명"은 원 등록/이전 개정 문구이며 **개정 1(권역)·개정 3(뼈대 PPS)·개정 4(핵심 108·예산 산술)·개정 5(Codex 교차검토 15건 반영, n=300 복원)**로 대체됨. 실행 규격은 **개정 5** 기준.

- **상태**: **사전등록 완료 (개정 5 재동결) — 데이터 대기** — 결정 D1~D4 승인: jinwoo, 2026-09-14. 설계 감사(AUDIT-2026-09-14-EXP011-design) → 2차 재감사(개정 4) → 3차 Codex 교차검토(`AUDIT-2026-09-14-codex-crossreview.md`, 개정 5). 블로커: 통계청 사회조사 MDIS 다운로드(사용자). 0단계(통계, $0)는 확보 즉시 착수, 1단계(LLM)는 0단계 게이트 통과 조합으로.
- **관련**: ISS-025(살 증강 방식 재검토 — 본 실험이 판정체) · ISS-022(실험 예외 승인, 2026-09-14) · ISS-023(분포 채널 표준) · EXP-008(뼈대만 예측 z=.10) · EXP-010(하류 채널) · `docs/2_전략기획/질문지고정_뼈대데이터_요청목록_v0.md`
- **결정 체인**: D1 (C) LLM 증강 암 포함(실험 예외) · D2 봉인 지역 경기·부산·전북 · D3 도너 = KGSS + 통계청 사회조사 블록 · D4 예산 ~$15, 1주 **+ 1단계에 SSR 채널 교차 포함**(jinwoo 추가 지시, 2026-09-14 — 상한 ~$20로 조정)

## 질문 (전부 가설 — 심판 명기)

광명 파이프라인 B단계("센서스 뼈대에 실존 도너의 350문항 살을 붙이기")는 광명에서 검증 불가(광명 350문항 정답 부재). 대신 **정답이 있는 시도를 통째로 숨겨 같은 절차를 총연습**한다.

- **H1 전이 가능성**: 뼈대 셀 매칭 도너 복사가 "전국 평균 붙이기"(바보 기준선)보다 봉인 지역의 문항 분포를 유의하게 잘 맞힌다. 심판: 문항별 TVD 평균, 3지역 모두 기준선 대비 부트스트랩 95% CI 분리. 실패 = 셀 전이 무의미 → 뼈대 기반 합성 재검토.
- **H2 결합·분산 보존**: 도너 통복사·블록 혼합이 문항별 대치("평균 인간")보다 문항 간 상관행렬 거리가 작고 셀 내 분산 보존율 ≥ 70%. 심판: 상관행렬 Frobenius 거리·분산비, 판별기 AUROC(문항별 대치·LLM 암은 ≤ 0.65).
- **H3 LLM 증강의 위치 (ISS-025 판정)**: (C)암이 도너 복사 대비 마진·결합·분산·하류 중 하나라도 유의하게 낫나. 심판: 같은 4지표. 이기면 ISS-022 supersede 안건, 지면 기각 근거.
- **H4 하류 동등성(B→C 연결)**: 가짜 살 페르소나가 진짜 살 페르소나와 분포 채널에서 같은 응답 분포·델타를 내나. 심판: 지역별 "가짜 300 vs 진짜 300" 분포 TVD ≤ 0.05, 워딩 델타 차 ≤ 3%p(페르소나 부트스트랩 CI).
- **부속 — 문항별 전이 지도**: 어떤 문항(영역)이 지역을 넘어 전이되고 어떤 것이 지역색인지 — 광명 질문 영역별 위험 경보기 + CIA 위반의 전형적 규모.

## 설계

### 0단계 — 통계 ($0, 2~3일)
- **봉인**: KGSS 4웨이브(2018·21·23·25) + 통계청 사회조사(최근 2주기) 응답자 중 시도 ∈ {경기, 부산, 전북}를 각각 통째로 봉인(정답지). 봉인 시도별로 독립 실행(다른 두 시도는 도너 풀에 남음).
- **뼈대**: 봉인 시도의 센서스 2% 성인 마진에 맞춰 500명 합성(`engine.bank` 빌더, 시드 고정).
- **살 채우기 암**(각 뼈대에 적용):
  | 암 | 방식 |
  |---|---|
  | A 도너 통복사 | 셀 사다리(성×연령10×학력×수도권×혼인 → 폴백, 셀당 도너 ≥30 강제) 내 무작위 도너 1명의 전 문항 복사. 다중 대치 m=5 |
  | A′ 블록 혼합 | 영역 블록(정치·사회자본 / 노동 / 가족 / 생활·건강 / 사회조사 블록)마다 같은 셀의 다른 도너 |
  | B 문항별 대치 | 릿지(EXP-008 규격) — "평균 인간" 대조군 |
  | C LLM 증강 | (C1) Nemotron-Korea 봉인 시도 페르소나 → 분포 발화로 350답 생성 / (C2) 우리 뼈대 → 동일. temp 0, 순서 무작위화. **실험 예외**(ISS-022) |
- **문항 선별 축**: 전체 vs 정규화 섀넌 엔트로피 상위 50%(Kinzinger 2026 처방).
- **측정**(봉인 실측 대비): ① 문항 TVD 평균(+바보 기준선) ② 상관행렬 거리 ③ 셀 내 분산비 ④ 판별기 AUROC ⑤ 문항별 전이 지도.

### 1단계 — LLM 하류 (gpt-4o-mini, 채널 2종 교차, ~$15)
- 0단계 게이트(H1·H2) 통과 조합 최대 3개 × 봉인 시도 3곳.
- 각 시도에서 "가짜 살 300명"(합성) vs "진짜 살 300명"(봉인 실측 응답자, 같은 뼈대 분포로 재가중) → 앵커 문항(총선 구도 A/B 문구) + 지역색 강한 문항 2개(전이 지도에서 선정).
- **채널 2종**: ⓑ 분포 발화 + 순서 무작위화(주 판정) · ⓒ SSR(선택지 비노출 서술 → 임베딩 앵커 측정기; 최소대립 문항은 증강 앵커 — EXP-010 사전분석 2 규칙). H4 판정은 채널별로 각각 보고, 두 채널이 엇갈리면 그 자체를 결과로 기록(측정기 의존성 증거).
- 프로필 형태: 350답 원문 나열(영역별 정렬). LLM 서사 층은 **미포함**(문헌상 원문 > 서사 — ISS-025). 예산 여유 시 서사 유/무 소규모 암 추가 가능(D4 상한 내).
- 콜 상한: 3지역 × 2판 × 300 × 2문구 × 3문항 × 2채널 × 조합 ≤3 ≈ 64,800콜 → 조합 수·문항 수로 $15 내 조정(풀 프로필 ~7k tok 지배). 채점 임베딩 ~$0.5.

### 동결 규칙
- 봉인 지역 실측은 0단계 채점 전까지 어떤 산출에도 사용 금지(도너 풀·튜닝 포함).
- 셀 정의·폴백 임계·엔트로피 컷·게이트 수치는 본 카드 기준, 실행 후 변경 금지(실행 불능 판명 시 개정으로 기록).
- (C)암 생성 모델과 1단계 시뮬 모델은 **다른 계열**(순환 차단): C 생성 = llama-3.2-90b(무료), 시뮬 = gpt-4o-mini.
- 재현: 시드 20260914, 모델 스냅샷·컷오프·프롬프트 버전·비용 기록.

## 사전 체크 (착수 전, $0)
1. 통계청 사회조사 마이크로데이터 확보(MDIS 공공용 — **사용자 다운로드 필요**: 로그인 계정) → 레지스트리 등록, 시도 코드·태도 문항 인벤토리.
2. KGSS↔사회조사 뼈대 변수 조화(성·연령·학력·혼인·지역) 매핑표.
3. 봉인 시도별 실측 표본 크기 확인(정답지 정밀도 — 웨이브·주기 합산).
4. 도너 풀 셀 계측 재실행(사회조사 포함) — `scripts/exp011_pre_donor_pool.py` 확장.
5. Nemotron-Korea 봉인 시도 필터 가능 여부·건수 확인(HF 데이터셋, CC-BY-4.0).
6. MDE: H4 등가마진(TVD .05 / 델타 3%p)에 필요한 n 모의계산.

## 개정 1 (2026-09-14, 실행 전 — 사전 체크 3·5 결과에 따른 실행 가능성 조정)
1. **봉인 단위: 시도 → 권역.** KGSS REGION은 시도가 아니라 **7권역**(서울/인천경기/강원/충청/경상/전라/제주). D2의 "경기·부산·전북"은 KGSS에서 식별 불가 → 봉인 권역 = **인천경기(4웨이브 1,302명)·경상(1,228, 부산 포함)·전라(503, 전북 포함)**. 통계청 사회조사(시도 코드)는 해당 권역 소속 시도 전부를 봉인해 단위를 맞춤. 함의: 총연습이 시도보다 한 단계 더 관대해짐(한계에 추가).
2. **뼈대 소스: 전국 뱅크 v1 부분집합.** 센서스 2% 원파일이 현재 PC에 없음 → 봉인 권역 뼈대는 `persona_bank_national_v1`(PPS 표집·사후층화 가중)의 해당 시도 레코드에서 500명 추출(인천경기 1,067·경상 1,347·전라 600 보유). 광명 전수 뱅크는 이미 존재하므로 EXP-011 범위에서는 원파일 불요(광명 뼈대 확장 시 필요 — 별도).
3. Nemotron-Korea: HF datasets-server로 `province`(시도 한글)·`district`(시군구) 필터 가능 확인 — (C1)암 봉인 권역 표본 추출 경로 확보.
4. 사회조사 마이크로데이터: MDIS 공공용은 로그인 다운로드 필요 → **사용자 작업**(블로커).

## 사전 체크 7 — 블록 간 결합(data fusion)의 CIA 위반 크기: KGSS 내부 자기검증 (2026-09-14, $0, `exp011_pre_fusion_test.py`)

KGSS 2023 응답자 1,210명은 정치 블록(12문항)과 신뢰 블록(12문항)에 **모두** 답함 → 진짜 블록 간 상관 = 정답(|r| 평균 .165, 최대 .494). 신뢰 블록을 다른 사람(도너)에게서 가져와 붙였을 때:

| 결합 방식 | 블록 간 \|r\| 보존율 | 부호 일치 |
|---|---|---|
| M0 무작위 도너 | 6% | 39% |
| **M1 인구셀(성×연령10×학력) 매칭 — "a,b,c 같으면 붙이기"** | **9%** | 67% |
| M2 + 일반 태도 6문항 최근접 | 15% | 86% |
| M3 + 표적 다리변수(종교참석·정치효능감·계층의식·생활만족·참여) | **16%** | 88% |
| M4 상한: + 신뢰 블록 자체 3문항 관측 | 56% | 92% |

- **판정**: 인구 속성으로 붙인 사람은 영역 간 결합의 **91%를 잃는다**(문헌의 "부분 상관 0 수축" 실측). 태도 다리변수를 넣어도 16% — 방향은 살아나지만(88%) 크기는 복원 안 됨. 사람의 영역 간 일관성은 관측 가능한 어떤 공통 변수에도 거의 실리지 않는 **미관측 잔차**다. 결합 대상 블록을 부분 관측(M4)해야 절반이 살아남음 — 그러나 우리가 다른 조사를 붙이는 이유는 정확히 KGSS가 못 재는 영역이므로 M4는 원리적으로 불가.
- **설계 변경(개정 2로 확정)**: ① "파운데이션 도너 = 여러 조사 조각 조립"은 **기각** — 사람의 척추는 KGSS 실존 1인(전 블록 동일인). ② 다른 조사(사회조사·경기도 사회조사)는 같은 사람에게 꿰매지 않고 **도메인별 별도 도너 인구**로 사용(복지·안전 질문 → 사회조사 도너 인구, 정치·사회자본 → KGSS 도너 인구) + 셀 수준 마진 보정·검증용. ③ 결합이 불가피한 경우(한 질문이 두 영역을 동시에 요구) 다리 조사 확보 전엔 이음매 상관을 **Fréchet 범위로 불확실성 보고**. ④ 1단계에 "결합 인물 vs 단일 도너 인물" 하류 대조암을 추가 — 영역 간 비일관성이 **집단 분포 시뮬**에 실제로 해를 끼치는지(개인 수준 ≈ 우연이므로 집단 수준에선 덜 해로울 수 있다는 가설) 실측.

## 개정 2 (2026-09-14, 실행 전 — 사전 체크 7 반영)
- 0단계 암 A′(블록 혼합)을 **"도메인별 도너 인구"**(질문 영역에 맞는 조사의 단일 도너)로 교체. 사람 단위 다중 조사 결합은 대조암(A″ 결합 인물)으로만 유지.
- 1단계 H4에 **H4b 결합 해악 검정** 추가: 결합 인물 vs 단일 도너 인물의 하류 분포 TVD·델타 차 — 등가마진 내면 "집단 시뮬에는 결합 비일관성이 무해", 밖이면 결합 인물 금지.
- 통계청 사회조사의 용도: KGSS 사람에게 덧대는 블록이 아니라 **복지·안전·생활 영역 질문의 독립 도너 인구 + 봉인 권역 셀 마진 검증**.

## 규격 확정 (2026-09-14 — 사전등록 완성분. 데이터 확보 즉시 0단계 착수)

### 매칭 셀 사다리 (동결)
L1 성×연령10×학력4×수도권×혼인 → L2 성×연령10×학력4×수도권 → L3 성×연령10×학력4 → L4 성×연령10. 셀당 도너 <30이면 다음 단계로 폴백(계측: L1 중앙 59명, 인물 35.6%가 <30 → 대부분 L2에서 해소). 도너 재사용은 허용하되 인물별 재사용 횟수 기록·보고.

### 살 채우기 암 (0단계, 봉인 권역별 독립)
| 암 | 정의 | 역할 |
|---|---|---|
| A 단일 도너 통복사 | 셀 내 무작위 도너 1인 전 문항, m=5 다중 | **기본** |
| A″ 결합 인물 | 정치·사회자본 블록은 KGSS 도너, 복지·안전·생활 블록은 사회조사 도너(인구셀+공유태도 매칭) | 결합 해악 대조(사전 체크 7: 보존율 16% 예상) |
| B 문항별 대치 | 릿지(EXP-008 규격) | "평균 인간" 대조 |
| C1 Nemotron-Korea | 봉인 권역 시도 페르소나 → llama-3.2-90b 분포발화로 350답 | LLM 증강(실험 예외) |
| C2 뼈대→LLM | 전국 뱅크 뼈대 → 동일 | LLM 증강(실존 뼈대판) |
| 문항 선별 축 | 전체 vs 엔트로피 상위 50%(`data/kgss_item_entropy.csv`, 관리성 문항 제외 후) | 전 암 교차 |

### 0단계 게이트 (동결)
- G-H1: 문항 TVD 평균이 바보 기준선(전국 분포 복사) 대비 3권역 모두 부트스트랩 95% CI 분리. 
- G-H2: 셀 내 분산 보존율 ≥ 70% **및** 상관행렬 Frobenius 거리가 B 대비 유의하게 작음. 판별기 AUROC(진짜 vs 가짜 인물, 로지스틱·5-fold) ≤ 0.65 — A/A″은 정보상 통과가 예상되므로 B·C에만 판정 적용.
- G-H3(ISS-025): C1/C2가 A 대비 4지표 중 하나라도 유의 우위면 "재검토 안건", 전패면 "기각 강화".
- 통과 조합 ≤3개만 1단계 진입.

### 1단계 (동결) — gpt-4o-mini 분포발화 + 순서 무작위화, SSR 교차
- 각 봉인 권역: 가짜 살 300(통과 조합별) vs 진짜 살 300(봉인 실측, 뼈대 재가중) → 앵커 A/B 문구 + 지역색 상위 2문항(0단계 전이 지도 기준).
- 게이트 H4: 분포 TVD ≤ .05 **및** 델타 차 ≤ 3%p, 부트스트랩 90% CI가 마진 내(TOST). H4b: A″ vs A 동일 마진 — 결합 비일관성의 집단 시뮬 해악 판정.
- MDE(사전 체크 6, `exp011_pre_mde.py`, EXP-010 인구암 앵커 실측 SD: 개인 델타 12.1%p·지원확률 2.6%p): 델타 차 등가마진 ±3%p TOST(부트스트랩 90% CI) 통과확률 — **n=300/300: 참 차이 0 → 0.85, 1 → 0.64, 2 → 0.27, 3 → 0.05, 5 → 0.00** / n=500: 0.97 / 0.84 / 0.38 / 0.05 / 0.00. 분포 TVD ≤ .05는 n=300에서 CI 반폭 ≈ .005로 충분. **채택: n=300(검정력 .85, 위양성 .05)**, 예산 여유 시 500.

### 어블레이션 계획 (2026-09-14 추가 — 게이트가 아니라 **지식 획득**용. 각각 가설·심판·얻는 지식 명기)

**0단계 어블레이션 ($0 — 통계 단계에서 전부 실행, 봉인 실측 대조)**
| # | 가설 | 조작 | 심판 지표 | 얻는 지식 |
|---|---|---|---|---|
| A1 살 두께 곡선 | 문항 수↑ → 결합·분산 보존↑, 수확 체감 | 문항 0/25/50/100/전체, 엔트로피 순 vs 무작위 순 | 상관행렬 거리·판별기 AUROC 곡선 | 광명 프로필의 **최소 필요 두께** — 350 전부가 필요한가 |
| A2 선별 기준 | 엔트로피 상위 > 무작위 > 엔트로피 하위 | 동일 수(50) 세 세트 | 위와 동일 + 문항 마진 TVD | "사람을 사람답게" 하는 문항의 성질(변별력 vs 안정성) |
| A3 결합 파괴 사다리 | 결합 손상↑ → 판별기↑ | 단일 도너 / 블록 결합(A″) / 문항별 셔플(같은 셀 내 문항마다 다른 도너 = 결합 완전 파괴) | 상관 보존율·AUROC | 결합 진실성의 **가치 크기** (EXP-008 SHUF의 통계판) |
| A4 도너 거리 | 도너 권역이 가까울수록 오차↓ | 도너 = 같은 권역 / 전국 / 반대 권역(수도권↔비수도권) | 문항 TVD 평균 | 전이 오차의 **거리 의존성** — 광명에 수도권 도너 우선이 맞는가 |
| A5 시간 시차 | 도너 웨이브가 멀수록 오차↑ | 2023 봉인을 2018/2021/2025 도너로 각각 재현 | 문항 TVD·문항별 시차 민감도 | 센서스 6년 시차의 **위험 크기**, 시간 민감 문항 목록 |
| A6 다중 도너 수 | m↑ → 분산 보존 안정, 수확 체감 | m=1/3/5/10 | 분산 보존율 분산·오차 막대 폭 | 생산 파이프라인의 m 기본값 |
| A7 셀 폭 | 세밀 매칭이 실제로 도움되나 | L1~L4 고정(폴백 없음) | 문항 TVD·도너 재사용률 | 매칭 정밀도 vs 도너 풀 크기의 최적점 |

**1단계 어블레이션 (LLM, 예산 $20 내 — 우선순위 순, 예산 소진 시 뒤에서 자름)**
| # | 가설 | 조작 | 심판 | 얻는 지식 |
|---|---|---|---|---|
| B1 지역 라벨 효과 | 지역 사실("인천경기 거주")만으로 LLM 응답이 움직이며, 그 방향이 실측 지역 차이와 다를 수 있다(고정관념) | 같은 도너 프로필에 봉인 권역 사실 삽입 vs 제거 vs **오답 권역** 삽입 | 응답 이동 방향·크기 vs 봉인 실측 권역 차이 | **지역 고정관념 검출기** — 광명 라벨을 넣을 때 무엇이 일어나는지 |
| B2 결합 가치(하류) | A3 사다리가 하류 분포·델타에 영향 | 단일/블록결합/셔플 인물 → 분포발화 | H4 지표 | 집단 시뮬에 결합이 필요한가(개인≈우연이므로 무해 가설 vs 유해 가설) |
| B3 살 두께(하류) | A1 곡선의 하류판 | 문항 0/50/전체 | 델타·TVD | 분포 채널에서도 정보 과포화가 있는가 |
| B4 C암 채널 | Nemotron 논문의 실패는 단일답 채널 탓 | C1을 단일답 vs 분포발화로 생성 | 350답 마진 TVD·분산 | 문헌 결과의 **재해석 근거** |
| B5 순서 위약 재검 | 순서 무작위화가 위치 편향을 제거 | NUKPLT18 순서 위약, 무작위화 on/off | \|Δ\| ≤ 5%p | 위생층 효과 확정(ISS-023 표준의 근거) |
| B6 프로필 형태 | 원문 나열 ≥ 서사(문헌) 한국어 재검 | 나열 / 도메인 정렬 / LLM 서사(검증기) | H4 지표·개인 AUC | 한국어에서 문헌 결과 재현 여부 |

원칙: 어블레이션은 게이트 판정에 미산입(탐색). 각 결과는 카드 "결과" 절에 가설별로 지지/기각/미결로 기록하며 실패 사례 동반. 0단계 7종은 전부 $0이라 전수 실행, 1단계는 B1→B6 순.

### 일정·비용
0단계 3일(사회조사 확보 후) → 1단계 2일 → 채점·감사 1일. 비용 상한 $20(1단계 ~$15 + C암 무료 + 임베딩 <$1).

## 개정 3 (2026-09-14, 실행 전 — 착수 전 설계 감사 `AUDIT-2026-09-14-EXP011-design.md` 반영)
1. **뼈대 PPS 재추출**: 전국 뱅크 v1의 시도 플로어(200)로 권역 내 시도 비율이 왜곡되므로 권역 뼈대 500명은 `weight_bank` 확률비례로 재추출, 추출 후 성×연령×시도 마진을 센서스 공표치와 대조(TVD ≤ .03).
2. **봉인 누출 assert**: 권역→시도 집합 매핑을 조화표에 고정, 도너 풀에 봉인 시도 코드 0건을 0단계 첫 단계에서 자동 검사.
3. **엔트로피 선별 층화**: 척도 크기(2/3/4/5+)별로 정규화 엔트로피 상위 50% 선별(이진 문항 편애 방지).
4. **H1 효과 크기 하한**: 유의성에 더해 바보 기준선 대비 TVD 개선 ≥ 0.02.
5. 1단계 발사 전 정확 콜 수·비용 기입. 1단계 결과로 0단계 규격 소급 변경 금지(동결 규칙 추가).
6. C1 프롬프트에 권역 실측 분포 노출 금지(ISS-003) 검토 항목화.

## 개정 4 (2026-09-14, 실행 전 — 2차 감사 `AUDIT-2026-09-14-EXP011-012-reaudit.md` 반영. **재동결**)
1. **기본 프로필 = 핵심 108문항**(2018·21·23·25 4웨이브 공통, `kgss_item_inventory.csv` n_waves=4). 도너·봉인 응답자·가짜/진짜 살 전부 동일 문항 세트. 모듈 문항(웨이브별 ~290)은 어블레이션 A1(살 두께)에서 2023 단일 웨이브로만 사용.
2. **C암 규격**: 핵심 108문항, 권역당 200명, **12문항/콜 배치**(분포발화, JSON 문항별 분포) → 9콜/명, 1,800콜/암/권역, C1+C2 × 3권역 = **10,800콜**(llama-3.2-90b 무료, 9~18h). 배치 호출은 '생성'이지 '예측'이 아니므로 EXP-008 문항별 호출 규칙과 목적이 다름을 명기. 문항 순서·선택지 순서 무작위.
3. **1단계 규모·예산 산술**: 권역당 가짜/진짜 각 **200명**, 문항 = 앵커 A/B + 지역색 1문항(2형) → 권역·조합·채널당 200×2판×2문항×2형 = 1,600콜; 3권역 × 조합 3 × 채널 2 = **28,800콜**; 108문항 프로필 ≈2.5k tok → **≈$11** (+SSR 임베딩 <$1). 상한 $20 내. 진짜 살 n = min(200, 봉인 가용).
4. **0단계 이중 실행**: KGSS 도너 인구(108문항)와 통계청 사회조사 도너 인구(사회조사 태도 문항)를 **각각** 봉인·대조·게이트 판정. 1단계는 KGSS 인구로만(앵커 문항이 KGSS 계열).
5. 시점 혼재(4웨이브 합산 도너·응답자에게 2023 앵커 시뮬)는 가짜/진짜 동일 조건이므로 H4 대조는 유효, 수준 지표는 보고하지 않음.
6. 문서 정합: 상단 결정 체인 D2·0단계 절의 "시도·센서스 2%"는 개정 1·3으로 대체됨(각주). 상태: **사전등록 재동결 — 데이터 대기**.

## 개정 5 (2026-09-14, 실행 전 — Codex(GPT) 교차검토 `AUDIT-2026-09-14-codex-crossreview.md` #1~17 반영. **재동결**)
1. **선별·계측의 봉인 누출 차단**(#1): 엔트로피 순위(`kgss_item_entropy.csv`)·셀 지원 계측·바보 기준선(전국 분포)은 **fold별로 봉인 권역을 제외한 도너 자료**에서 재계산. 사전 체크 7(전체 2023 자료)은 설계 변경의 개발용 근거로만 인용, 봉인 증거 아님. 판정 문구도 "이 매칭 규격(12×12 문항·다리변수 5·최근접 5·희소 셀 전국 폴백)에서 복원 실패"로 한정(#15).
2. **평가 문항 프로필 제외 목록 동결**(#2): 1단계 앵커·지역색 문항 및 동일 개념 문항은 도너·진짜 살·C 생성 프로필 **전부**에서 제거(진짜 살에 정답이 그대로 들어가는 누출 차단). 목록은 0단계 전이 지도 확정 직후, 1단계 발사 전 커밋.
3. **1단계 대조 추가**(#3): 뼈대만(살 0)·문항 셔플(같은 셀 내 문항마다 다른 도너) 인물. H4 통과 조건에 "가짜 살이 뼈대만 대비 유의하게 진짜 살 쪽으로 이동" 병기 — 살 무시로 인한 가짜 동등 배제.
4. **A″ 채점 범위**(#4): KGSS×사회조사 공통 응답자가 없으므로 A″의 결합 상관은 정답 부재 → G-H2 판정 제외, **H4b(하류)로만** 판정. Fréchet 보고 규격은 다리 조사 확보 후 별도 개정.
5. **n=300 복원 + 검정력 재계산**(#5, 개정 4 오류): n=200 TOST 통과확률 참차이 0/1/2/3%p = .62/.48/.20/.06(재계산, 정규근사 .59) → 부적합. 권역당 가짜/진짜 **300명**(검정력 .83). 콜 수 유지를 위한 암·문항 재배치: 진짜 살 300×2문항×2형 = 1,200(조합 간 **공유**) + A 1,200(2문항) + A″ 600(앵커만) + C/B 600(앵커만) + 뼈대만 600 + 셔플 600 = 권역·채널당 4,800 → **28,800콜**. 비용 = 입력 ≤72M tok $10.8 + 출력(~120 tok) $2.1 + SSR 출력 가산 ~$1 + 재시도 10% ≈ **$15**(상한 $20). TVD 검정력의 1.5× 계수는 임의였음(#6) → 0단계 가짜 살 확률벡터로 재산정 후 1단계 발사.
6. **불확실성 단위**(#7): 도너 단위 재표집(페르소나 아님), 고유 도너 수·유효표본수(ESS)·대치 간 변동 보고. m=5는 별도 인구로 유지(평균 인간으로 합치지 않음).
7. **H4b 3분 판정**(#8): 90% CI ⊂ 마진 → 동등 입증 / CI가 마진 밖에 전부 → 실질 차이 입증(결합 인물 금지) / 걸침 → 미결(금지 아님, 재검정력). 핵심 하위집단(성×연령대) 수준·델타 병기.
8. **C암 개인 응답 규칙**(#9): 문항별 분포에서 대치 m마다 1답 추출 → 개인 공동응답. 12문항 배치 내 결합만 보존, 배치 간 독립을 한계 명기. 주변분포 성과와 공동분포 성과 분리 보고. A·B·C 비교 단위 = 추출된 개인 응답 행렬로 통일.
9. **판별기**(#10): 로지스틱(선형) + GBM(상호작용) 병행, 전처리·튜닝 fold 내, 도너 복제본은 동일 fold, AUROC 95% 상한으로 판정. A/A″ 면제 철회 — 전 암 보고, 게이트 적용은 B·C.
10. **H1·H3 재량 제거**(#11): H1 = paired(문항별) TVD 개선량의 부트스트랩 CI 하한 > 0 **및** 점개선 ≥ .02. H3 = Holm 보정(2암×4지표×3권역=24검정), 우위 지표 외 지표 악화 ≤ 기준선 대비 .02. 1단계 진입: A·A″ 필수(#12) + C1/C2 중 G-H3 통과 암 1개(둘 다면 4지표 합산 우위, 없으면 B).
11. **셀 지원 사전 계측**(#13): fold별 사다리 도달률·미지원 질량·고유 도너 수를 0단계 첫 단계에서 산출·커밋. L4 < 30 → **L5 = 성×연령10 전국(수도권 무시)**, L5도 미달이면 해당 인물 기록 후 전국 무작위(비율 보고).
12. **웨이브 조화**(#14): 108문항의 웨이브별 문항 문구·선택지·결측코드 조화표 커밋, 결과는 웨이브별 병기. A5는 전향(과거 도너→2023)·후향(2025 도너→2023) 분리 보고.
13. **어블레이션 척도 고정**(#16): A1/A2는 공통 평가 문항 세트(전이 지도 상위 50)로 채점, Frobenius는 문항 수 정규화. 0문항 조건은 판별기·TVD만.
14. 사전 체크 7 판정 문구 한정(#15) — 항목 1 참조.
15. **절차 교훈**: 단일 모델 자기감사 2회가 위 15건(중대 6건)을 놓침 → 동결·판정 전 교차 모델 검토를 기본 절차로 제안(ISSUES.md 규칙 — 인간 결정 대기).

## 알려진 한계(등록 시점 + 개정 5)
권역(수백만)은 시군구(광명 28만)보다 관대한 시험 · KGSS 권역 표본 소형(전라 503) · 사회조사 지역코드 시도 한정 · 4웨이브 합산 시점 혼재 · 2020 센서스 시차 · 개인 재현은 채점 대상 아님(집단·구조·하류만) · 광명 고유성은 본 실험 밖(경기도 사회조사 원자료 확보 후 후속 EXP: 광명 마진 대조).

## 결과 / 판정 (실행 후 기입)
