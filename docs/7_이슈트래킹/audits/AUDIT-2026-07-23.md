# 감사 기록 — 2026-07-23 T2 페르소나 엔진 적대적 감사 (완료 후 결과 감사)

> 대상: `src/engine/{registry,harmonize,tables,bank}.py` + `scripts/{build_bank,validate_bank}.py` + 뱅크 v0 (EXP-002)
> 방법: 레드팀 공격 테스트 배터리 A~H **실행 증거 기반** (AGENTS 구현 규율 3축: 동작·게이트 커버·회귀)
> 수행: 에이전트(Claude) 단독 — **인간 결정자 승인 대기 항목은 ISS-016 참조** (AGENTS 규칙 8)

## 1. 공격 배터리 결과

| # | 공격 | 결과 | 심각도 | 처리 |
|---|---|---|---|---|
| A | KGSS 성별 코딩 가정(1=남 2=여) — 코드가 검증 없이 가정 | 값 라벨 실측: 1=남자, 2=여자 — **가정 옳음** | — | 확인 종결 |
| B | 지역코드 체계 — LAF 4자리 앞 2자리가 센서스 시도와 다른 체계일 가능성 | 17개 prefix 완전 일치 실측 | — | 확인 종결 |
| C | **급여 조회표에 15~17세 오염** (LAF는 15세+, 뱅크는 18세+) | 급여 표본 163,372 중 미성년 90명(0.06%) 혼입 확인 | P2 | ✅ 수정 — 성인 필터 추가 (`tables.py`) |
| D | **NaN 진리값 잠복 크래시** — `if row[...]` 패턴은 float('nan')이 truthy | 현 데이터는 통근자 결측 0이라 우연히 통과. 데이터 변형 시 크래시 재현 가능 | P1(잠복) | ✅ 수정 — `_s()` NaN-안전 가드 전면 적용 (`bank.py`) |
| E | 재현성 — 동일 시드 재빌드가 저장본과 달라질 가능성 | sha256 완전 일치 (골든 테스트 통과) | — | 확인 종결 |
| F | 조회표 확률 합 ≠ 1 | 전 셀 0건 | — | 확인 종결 |
| G | 뱅크 ID 중복·필수 필드 결측 | v0 양 트랙 중복 0·결측 0 | — | 확인 종결 |
| H | **가중치 의미론 — 이중가중 편향** | PPS 추출 후 `인구가중값` 재가중 시 분포 왜곡 실측(성비 51.2→50.2). 층화 플로어 확장 시 무가중 집계도 오답이 됨 | **P1** | ✅ 수정 — `weight_bank`(사후층화 가중치) 신설, 검증 스크립트 가중 집계 전환. 설계 결정은 **ISS-016** 카드로 |

추가 발견(코드 정독): ISS-010 이념 게이트가 **문서로만 존재, 코드 미강제** (P2) → ✅ `--ideology {dev,off}` 플래그로 강제(기본 dev는 provenance에 게이트 상태 기록, off는 미포함). registry sha256 검증 미사용 (P3) → ✅ `--verify` 플래그. 플로어 초과분 차감이 최대 시도(서울·경기)에 집중되는 배분 특성 (P3) — 가중 집계로 상쇄되므로 수용, 기록만.

## 2. 게이트 커버 대조 (축 ii — ISSUES.md)

| 게이트 | 커버 상태 |
|---|---|
| ISS-002 경로 하드코딩 금지 | ✅ registry.json + IDEA_DATA_DIR (+ --verify 해시 검증) |
| ISS-003 x만 주고 P(y) 금지 | ✅ 카드에 페르소나 사실만 — 집계 통계·행동 지시 없음 |
| ISS-010 이념 라벨 인간 게이트 | ✅ 코드 강제(--ideology) + provenance 기록. **게이트 통과 자체는 미완** — 인간 이중코딩은 후속 |
| 최빈값 금지 (AGENTS 불변) | ✅ 전 속성 분포 추첨 |
| T2 완료 기준 (시도 n≥200·마진 ±1.5%p) | ✅ v1에서 충족 (아래 §3) — 단 라벨 κ 게이트(ISS-010)는 미완이므로 **T2 전체 완료 선언은 보류** |

## 3. 수정 반영 후 재검증 (v1 확장 결과)

| 트랙 | n | 구성 | 가중 마진 최대편차 | 시도 편차 |
|---|---|---|---|---|
| 전국 v1 | **5,000** | 시도 플로어 200 (전 시도 충족, min=200) + 가중 비례 | **1.09%p** (±1.5%p 게이트 내) | **0.0%p** (사후층화로 정확 복원) |
| 광명 v1 | **4,568** | 성인 풀 **전수** (표집 없음, 센서스 가중값 승계) | **0.0%p** (전수라 정의상 일치) | — |

- weight_bank 범위 0.156(플로어 소형 시도)~1.626(대형 시도) — **집계는 반드시 weight_bank 가중** (무가중 시도 분포는 의도된 과대표집)
- 성향 가중 분포(전국): 진보 31.4 / 중도 37.9 / 보수 27.5 / DK 3.2 — 붕괴 없음
- 광명 급여의 99%가 광명 자체 셀(L1) 추첨 유지

## 4. 최종 판정

**조건부 통과** — P1·P2 전량 수정 및 재검증 완료. 코드·뱅크 v1은 다음 단계(T3 위생 실험) 투입 가능.
잔여 전제: ① **ISS-016 인간 기명 결정**(뱅크 가중치 설계 채택), ② ISS-010 인간 이중코딩(이념 라벨 프로덕션 사용 전), ③ v0 뱅크는 이중가중·오염 수정 전 산출물이므로 **후속 실험 사용 금지, 기록 보존용**(EXP-002 주석).

## 5. 문서로 못 없애는 잔여 리스크

- 가구소득 조건부의 '가구주 근사'(검증리포트 v0 §6-3) — 후속 개선 항목 유지
- KGSS 무가중 사용·권역 7 한계 — 리포트 명시 유지
- 시군구 이종 코드 매핑이 광명 1건 — 타 시군구 확장 시 매핑표 선행
