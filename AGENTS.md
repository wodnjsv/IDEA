# AGENTS.md — IDEA 프로젝트 에이전트 규칙 (정본)

> 이 파일이 모든 코딩 에이전트(Codex, Claude Code, Cursor 등)의 규칙 정본이다.
> Claude Code는 CLAUDE.md의 `@AGENTS.md` 임포트로 이 파일을 읽는다. 규칙 수정은 이 파일에서만.

## 프로젝트 한 줄
공공데이터 + LLM 합성 인구로 정책 여론/반응을 시뮬레이션하는 **검증형 예측 엔진**(한국형 Aaru).
상품은 단일 숫자가 아니라 **조건(프레이밍) 민감도**와 **정확도 리포트**(어디서 얼마나 틀렸는지 공개).

## 필독 문서 (작업 유형별)
- 전체 지도: `docs/README.md`
- **이슈·결정·실험 인덱스: `docs/7_이슈트래킹/ISSUES.md`** ← 모든 작업 전 확인
- **태스크·주차·예산 정본: `docs/2_전략기획/구현계획_v1.md`** ← 구현 착수 기준(T1-T10)
- 방법론 근거: `docs/2_전략기획/오차축소_방법론_비교분석.md` (주차 표기는 구버전 — 구현계획이 정본)
- 기존 코드 진단: `phase0/구현분석.md`
- 정답지·백테스트: `docs/6_백테스트/백테스트_그라운드트루스.md`

## 이슈 트래킹 규칙 (핵심 — 위반 금지)
1. **작업 전 인덱스 확인**: `docs/7_이슈트래킹/ISSUES.md`를 읽고, 하려는 작업이 DECIDED 이슈와 충돌하면 그 결정을 따른다. 뒤집으려면 새 ISS 카드를 만들어 원 카드를 `SUPERSEDED(by ISS-xxx)`로 표시하는 절차 없이 재론·무시 금지.
2. **발견 즉시 기록**: 구현·리뷰·실험 중 발견한 비자명한 문제/리스크/설계 갈림길은 `docs/7_이슈트래킹/issues/`에 ISS 카드로 생성한다(OPEN 상태라도). 템플릿: `_templates/TEMPLATE_ISS.md`.
3. **결정은 근거와 함께**: 결정 섹션에는 검토한 옵션·장단·근거 등급([강/중/약/추측])·기각 사유를 남긴다. 결정만 덜렁 쓰지 않는다. DECIDED 이후 결정 섹션은 append-only.
4. **구현 역링크**: 이슈 관련 커밋 메시지에 `(ISS-NNN)`을 포함하고, 카드의 구현 섹션에 커밋 해시·파일 경로를 기록한다.
5. **검증 없이 VERIFIED 금지**: 수치·테스트·게이트 통과 증거를 검증 섹션에 기록해야만 VERIFIED로 올린다.
6. **실험은 EXP 카드 필수 — 캐시 재채점 포함**: **지표를 산출하는 모든 실험 런**(LLM 호출 없는 캐시 재채점·재가중 포함)은 EXP 카드(가설→설정→결과→판정) 없이 돌리지 않는다(캐시 런은 경량 양식 허용 — 가설·설정 diff·결과만). 재현 정보(모델 **스냅샷 버전**·**컷오프**·시드·temperature·표본수·프롬프트 버전·비용) 필수. 좋은 숫자만 고르는 체리픽 금지 — topline+하위집단+실패 사례 동반 보고.
7. **인덱스 동기화**: 카드 생성·상태 변경은 같은 커밋에서 ISSUES.md 표를 갱신한다.
8. **결정 권한**: DECIDED 선언·SUPERSEDE는 **인간 결정자 기명 필수**(에이전트 단독 선언 금지). 에이전트는 ANALYZED까지 올리고 결정을 제안만 한다.
9. **채번**: ISS/EXP는 별도 카운터, 3자리 패딩, 마지막 번호+1. 브랜치 충돌 시 후발 브랜치가 재번호.

## 프로젝트 불변 원칙 (설계 제약)
- **조건 민감도 보존**: 어떤 보정/기법도 프레이밍 델타를 뭉개면 안 된다(출력 하드클램핑 금지). 채택 게이트: 2×2 델타 불변성 — **TOST 규격(등가 마진·α·검정력) 명기된 게이트만 유효**(ISS-015).
- **x는 주고 P(y)는 주지 않는다**: 프롬프트에 페르소나·시나리오 사실은 허용, 응답변수의 집계 통계·행동 지시는 금지 (ISS-003).
- **최빈값 집계 금지**: 반복 응답은 분포로 보존한다 (근거: 오차축소 §3 B3 — CoT-SC 다수결 수축 금지. 채널 선택은 ISS-004에서 A/B로 별도 판정).
- **홀드아웃·봉인 규율**: 보정에 쓴 데이터로 채점하지 않는다. **지정 개발셋(21대 대선 등)은 반복 채점 허용**(단 그 수치를 대외 성과로 인용 금지 — 개발 지표일 뿐). **봉인 1회 채점 규율은 지선·4단 리허설·미래 사건에 한정**(사전등록 커밋 동결 후 1회, 결과 후 수정 금지 — ISS-009). "홀드아웃" 명칭은 봉인 문항 셋·미래 사건에만. 비-LLM 베이스라인(G9) 병기.
- **컷오프 게이트**: 채점 대상 선거일보다 컷오프가 늦은 모델의 백테스트 결과는 성과로 인용 금지(MEMORIZATION_RISK — ISS-012).
- **비용 게이트**: API 예산 상한 **$500/학기**. 풀런(생성) 발주 전 EXP 카드에 예상 비용 기입 필수, 대량 런은 Batch API, 어블레이션·파일럿은 1-2천 명 서브샘플 기본 (ISS-014).
- **paired 규약**: 프레이밍 실험은 동일 페르소나 차분 + MDE 표 사전 계산 필수. "동일 시드" 재현성 주장 금지 (ISS-011).
- **법적 경계**: 선거 여론 수치의 대외 공표 금지(공직선거법 §108조의2) — 내부 자문·방법론 검증 한정. 제보(PII)는 ISS-008 법적 검토 전 실제 데이터 사용 금지.
- **정직 보고**: 실패·미검증은 실패·미검증이라고 쓴다. 이 프로젝트의 브랜드가 "틀린 지점 공개"다.

## 구현 규율 (코드 변경 시 — 위반 금지)
- **적대적 감사 필수**: `src/` 신규 모듈·비자명한 코드 변경(phase0 재작성 포함)은 (a) 착수 전 설계 감사 + (b) 완료 후 결과 감사 — 독립 리뷰어/레드팀 관점의 적대적 검토를 수행하고, 그 결과를 `docs/7_이슈트래킹/audits/AUDIT-YYYY-MM-DD.md` 또는 관련 ISS 카드 검증 섹션에 기록한 뒤에만 **'완료'로 본다**. 오타·주석·포맷 등 자명한 수정은 예외.
- 감사는 최소 3축을 공격적으로 확인한다: (i) **의도대로 동작하는가**(실행 증거·테스트), (ii) **관련 이슈·게이트를 실제로 커버하는가**(ISSUES.md 대조), (iii) **새 문제·모순을 유발하지 않는가**(회귀·문서 정합).
- 코드가 이슈의 게이트를 구현하면, 그 ISS 카드를 IMPLEMENTED로 올리고 감사 통과 후에만 VERIFIED로 승급(규칙 5).

## 코드 컨벤션
- 경로 하드코딩 금지 — 데이터 접근은 레지스트리 경유(ISS-002), `IDEA_DATA_DIR` env → 기본 `repo_root/data`.
- 지역 조인은 행정표준코드 기준(이름 문자열 매칭 금지).
- 생성(LLM 호출)과 보정·채점을 단계 분리 — raw 응답은 항상 jsonl로 캐시, 보정 연구는 캐시 위에서.
- 커밋 메시지는 영어, 본문에 한국어 허용. 관련 이슈 ID 표기.

## 저장소 구조
```
docs/1_서울편입 … 7_이슈트래킹   # 문서 (README.md가 지도)
phase0/                          # 기존 실험 (EXP-001, 재작성 대상)
data/                            # (예정) 레지스트리 + 정답지
src/                             # (예정) engine/ · triage/ 분리 (PIPA blast radius)
```
