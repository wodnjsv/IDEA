# AUDIT-2026-09-02 — EXP-009·EXP-010 구현 결과 감사 (완료 후)

대상: `scripts/exp009_*.py`(prep/run/score/prechecks) · `scripts/exp010_*.py`(run/score/rate/pre) · 자동 체인 3종(ps1). 착수 전 설계 감사는 각 카드 본문에 분산 기록(EXP-009 개정 2 ①~④ 심사 기준, EXP-010 설계 "함정 방지" 절) — 본 문서는 완료 후 3축 감사.

## 축 1 — 의도대로 동작하는가 (실행 증거)

- 러너: EXP-009 70,560콜(KR 25,200 + US 45,360) 오류 최종 0, 파싱 실패 0/4건 수준. EXP-010 KR 9,000콜 파싱 실패 0, US dist 9,120콜 중 4. 재개 안전(done-키)은 절전 5회·네트워크 단절 1회·프로세스 재시작 6회에 걸쳐 데이터 중복·유실 없이 검증됨(행 수 = 예상 콜 수 일치).
- 파서: dist 분포 정규화(합≠1·음수·키 혼재) 스모크·본런에서 실패 4/18,240 — 재시도 소진분만 실패 기록.
- 자동 체인: EXP-009 US 완료 감지 → 채점 → EXP-010 US 발사 → dist 완료 → ssr 스모크 게이트(8/8) → ssr 본런 — 전 단계 무인 실행 확인(`us_chain_watch.log`).

## 축 2 — 이슈·게이트를 실제로 커버하는가 (대조)

- ISS-003(P(y) 미주입): 프롬프트 전수 코드 검토 — 실측 분포·기관명 미노출. 분포 발화는 "수신"이라 통과(EXP-010 카드 명시). ✔
- ISS-011(paired): 동일 페르소나 A/B 독립 호출 — 키 구조로 강제. ✔
- ISS-012(컷오프): 4o-mini 2023-10 < 앵커 공개 2023-11 / llama-3.2-90b 2023-12 < 코퍼스 공개 2025-08 + 암기 프로브 17/17 UNKNOWN. ✔
- ISS-014(비용): 총 지출 ≈ $14(KR 6.55 + 6 + 채점자 ~1) — 상한 $500 내. ✔
- ISS-015(델타 게이트): EXP-010은 델타 뭉갬 여부 자체가 판정 대상 — 하드클램핑 없음. ✔
- 사전등록 게이트 준수: EXP-009 게이트 변경은 전부 실행 전 개정(1~3)으로, EXP-010 게이트는 발사 전 동결로 기록. 결과 후 소급 변경 0건. 단 **부문② "유의 개수" 게이트의 설계 결함**(정밀도 의존)이 실행 후 판명 — 게이트 자체는 문구대로 채점(불합격 기록)하고 결함을 별도 명시(사후 재해석과 구분). ✔

## 축 3 — 새 문제·모순을 유발하지 않는가 (발견 결함·수정)

- **[수정됨] role NaN 버그**: frozen CSV의 "null" 문자열을 pandas가 결측 파싱 → 채점기 KeyError 예정 경로. 사전 분석 중 발견, `keep_default_na=False`로 수정·runtime 재생성·검증(실측 17/참고 1/위약 20/무프로필 10 재확인). 커밋 407fa16.
- **[수정됨] EXP-009 US 러너 커넥션 고착**: 네트워크 단절 복구 후 러너가 죽은 커넥션으로 재시도 소진(537행) — 재시작으로 회수. 후속 체인에 자동 재시작(≤2회) 내장.
- **[기록] 프로필 풀 노이즈**: KR 풀 프로필에 "면접 일: 11일" 등 관리성 항목 소수 잔존 — 필터 미비. 결과 반전 수준 아님(양 채널 동일 조건), 후속 prep에서 제외 목록 보강 필요.
- **[기록] 한국 SSR τ in-sample**: τ가 인구암-A TVD 최소로 캘리브레이션 — 해당 칸 자기 채점. B형·타 암·dist(무캘리브레이션)의 독립 성적으로 일반화 뒷받침. 카드에 명시됨.
- **[미해결] llama dist 감쇠·waq4m류 대형 오탐 2건** — ISS-023 결정(보정층)의 입력으로 이관.

## 종합

구현은 사전등록 규격을 준수했고, 발견된 결함 2건은 결과 산출 전 수정, 나머지는 카드·이슈에 정직 기록됨. EXP-009는 VERIFIED 승급 요건(수치·게이트 증거) 충족 — 판정이 "불합격"인 것과 검증 완결은 별개. EXP-010은 미국 SSR 완료 후 재감사 1회 예정.
