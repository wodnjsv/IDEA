# ISS-019 — 추론 백엔드 이중화 (NVIDIA NIM 무료 티어)와 비교가능성 게이트

- **유형**: 코드·사업(비용) · **P**: P2 · **상태**: **VERIFIED** (2026-08-07 실 API 프로브 통과 — §검증. 모델 최종 확정만 인간 결정 대기)
- **관련**: ISS-012(컷오프 게이트) · ISS-014(비용 게이트 $500) · ISS-011(paired 규약) · AGENTS 규칙 6(모델 스냅샷 기록)

## 문제
EXP-004·005를 유료 OpenAI로 돌려 누적 ~$1.3을 썼다. T10 어블레이션·2단 보정은 반복 실행이 전제라 유료 유지 시 ISS-014 예산이 빠르게 소모된다. NVIDIA NIM 무료 티어(`https://integrate.api.nvidia.com/v1`, OpenAI SDK 호환)로 옮기면 비용은 $0이 되지만 **세 개의 새로운 게이트 위험**이 생긴다.

1. **컷오프 오염(치명)** — 무료 카탈로그의 간판 모델(2025년 이후 학습분)은 21대 대선 결과를 이미 알고 있다. ISS-012 게이트에서 전량 차단 대상이며, 통과하는 것은 오히려 **구형 모델**뿐이다. "성능 좋은 모델을 고른다"는 직관이 여기서는 정반대로 작동한다.
2. **비교가능성 파괴** — EXP-005의 D·E 판정은 EXP-004 B암(gpt-4o-mini)과의 paired 비교로 정의돼 있다(ISS-011). 백엔드만 바꾸면 암 간 차이에 **모델 효과가 교란**된다.
3. **병목의 이동** — 유료에서는 비용이, 무료에서는 **RPM(공개 한도 40 RPM, 모델 무관 계정 단위)**이 병목이다. 게다가 다수 NIM 엔드포인트가 `n>1`을 지원하지 않아 K=5 수집이 1콜→5콜로 늘면 요청 수가 5배가 된다.

## 결정 (코드로 강제한 사항)
- `src/engine/llm_client.py` 신설 — 백엔드·모델 레지스트리, 컷오프 게이트, 레이트리미터, K 샘플러를 한곳에 둔다.
- **컷오프 게이트를 기본 차단으로**: 컷오프가 선거일 이후이거나 **1차 출처로 확인되지 않으면 실행 자체를 막는다**(`--allow-memorization-risk`로만 강행, 그 경우 결과에 `MEMORIZATION_RISK` 라벨 강제). 종전 `assert MODEL_CUTOFF < ELECTION_DATE` 하드코딩보다 강한 규격.
- **n=K 자동 강등**: n=K 1콜을 먼저 시도하고 실패·부족 시 K콜 직렬로 내려간다. 판정은 1회만 하고 고착시킨다(페르소나마다 실패콜을 반복하지 않음).
- **원자료 모델 분리**: `raw_{arm}[_smoke][_{tag}].jsonl`. gpt-4o-mini는 tag 없음 → 기존 EXP-004/005 산출물과 파일 충돌 없음. 각 레코드에 `model` 필드를 남긴다.
- **모델 혼재 경보**: `score_ab.py`가 암별 `model`을 대조해 혼재 시 "paired 무효" 경고를 출력한다.
- **키 취급**: 환경변수 우선, 없으면 gitignore된 키 파일에서 정규식 추출. 로그에는 `nvapi-…(API.txt, 70자)` 형태의 지문만 남기고 키 문자는 한 글자도 출력하지 않는다.

## 잔여 리스크 (문서로 못 없앰)
- 컷오프 근거는 **모델카드 등 2차 공개문서**다. 학습 데이터 실물 감사는 불가능하므로 "컷오프 이전"은 강한 주장이 아니라 **최선의 방어**로만 기술한다. 반증 도구는 EXP-006의 사후 지식 프로브(§검증)로 남긴다.
- 무료 티어 한도(40 RPM)는 NVIDIA가 공식 표로 보증한 값이 아니라 계정·모델·혼잡도에 따라 변동한다는 것이 개발자 포럼의 모더레이터 답변이다. 리미터 기본값을 35로 두되 429 관측치를 실행 로그에 남긴다.
- **한국어 역량**은 후보군(Llama·Gemma·Qwen)의 공식 지원 언어 목록과 무관하게 실측해야 한다 → `scripts/probe_nvidia.py`.

## 다음
프로브 실행 → 모델 확정(인간 결정) → EXP-006(교차모델 복제)로 유료↔무료 등가성 판정. 등가 확인 전까지 **성능 주장은 gpt-4o-mini 결과로만** 한다.

> **모델 확정 (2026-08-07, 결정자: jinwoo — 세션 대화)**: 무료 백엔드 기본 모델 = **`llama-3.1-70b`** (meta/llama-3.1-70b-instruct). 근거: 실측 프로브(n=5 1콜·파싱 3/3·한국어·지연 26s — 생존 3종 중 유일한 실용 후보) + 지식 프로브 5%(컷오프 정합). 등가성 판정(EXP-006)은 별도.

## 구현 (Implementation)

커밋 `0f32580` — feat: NVIDIA free-tier inference backend + score EXP-005 card-layer A/B

| 파일 | 역할 |
|---|---|
| `src/engine/llm_client.py` (신규) | `PROVIDERS`/`MODELS` 레지스트리, `load_key()`(env 우선·지문만 출력), `assert_cutoff()`(ISS-012 강제), `scrub()`(로그·산출물 키 마스킹), `clean()`(`<think>`·코드펜스 제거), `RateLimiter`(35 RPM 기본·429 페널티), `Sampler`(n=K 자동 탐지·sticky 강등·`_probe` 락), `add_backend_args()`/`resolve_backend()` |
| `scripts/probe_nvidia.py` (신규) | 후보 모델 실측 — 호스팅 여부·`n=K` 지원·지연·JSON 준수·투표 파싱·한국어. 랭킹 후 추천 + 벽시계 추정 출력, `data/t3/nvidia_probe.json` 기록 |
| `scripts/selftest_llm_client.py` (신규) | 스텁 클라이언트 21개 검사 — API 0콜로 배선 전체 회귀 |
| `scripts/run_ab_cards.py` (수정) | `--arms B D E`(B 재실행 = paired 복원), `--provider/--model/--tag`, `raw_{arm}[_smoke][_{tag}].jsonl`, 레코드에 `model`·`gate`, 오류 문자열 `scrub` |
| `scripts/run_t3_pilot.py` (수정) | A/B/C 암에 동일 백엔드 스위치. 하드코딩 `MODEL`·`assert` 제거(게이트는 `llm_client`로 일원화) |
| `scripts/score_ab.py` (수정) | `--tag`, 페르소나 단위 dedup, 암별 `models`/`gates` 대조 → 모델 혼재 시 **paired 무효 경고**, `n_error` + 미수집 1퍼센트 경보, MEMORIZATION_RISK 경고 |

## 검증 (Verification)

- `scripts/selftest_llm_client.py` — **21/21 통과** (API 0콜). 커버: n=K 배치 정상/거부(400)/조용한 축약, 429 백오프·페널티, 컷오프 게이트 6케이스, 리미터 간격 실측, `clean()`, 키 문자열 비노출, `scrub()` 마스킹
- 회귀: 백엔드 층 교체 후에도 기존 gpt-4o-mini 원자료를 동일 수치로 재채점 (B MAE 5.93 / D MAE 11.13)
- 적대적 감사: [audits/AUDIT-2026-07-27.md](../audits/AUDIT-2026-07-27.md) — P1 2건·P2 3건 수리, **조건부 통과**
- 무료 티어는 비용 $0이므로 ISS-014 예산 게이트는 자동 충족. 대신 **RPM 게이트**가 새로 생김 — 러너가 요청 수·예상 소요시간을 사전 출력한다
- ✅ **VERIFIED 승급 (2026-08-07, 실 API 프로브)**: `probe_nvidia.py` 실측 — 카탈로그 100종 조회, 후보 7종 중 3종 생존, **llama-3.1-70b: n=5 1콜 지원·지연 26.1s·JSON/투표 파싱 3/3·한국어 정상**(→ `data/t3/nvidia_probe.json`). 레이트리미터·키 지문(`nvapi-…, 70자`)·scrub 전부 라이브에서 정상 동작. **지식 프로브(EXP-006 사전등록 절차) 1/20=5%** — 문서 컷오프(2023-12)의 경험적 정합 확인(→ `data/t3/knowledge_probe_llama-3.1-70b.json`). 잔여: 모델 최종 확정(인간)·등가성 판정(EXP-006 본런). 발견: gemma-2/3·qwen·405b가 무료 카탈로그에서 제거됨 — 무료 티어 카탈로그 변동성이 실측으로 확인된 신규 리스크(§잔여 리스크의 "40 RPM 비보증"과 동계열)
